import route from './client/route'
import bootstrap from './client/bootstrap'

export default {
  route,
  routes: [
    {
      tagname: '{{ name }}-main',
      page: '{{ name }}-main'
    }
  ],
  bootstrap
}
