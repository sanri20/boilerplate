import { getRepository, In } from 'typeorm'
import { {{pascalCase name}} } from '../../../entities'

export const delete{{pluralPascalCase name}} = {
  async delete{{pluralPascalCase name}}(_: any, { names }, context: any) {
    const { domain, user } = context.state

    await getRepository({{pascalCase name}}).delete({ 
        domain,
        name: In(names)
    })
    
    return true
  }
}

