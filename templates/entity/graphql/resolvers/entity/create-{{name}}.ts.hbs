import { getRepository } from 'typeorm'
import { {{pascalCase name}} } from '../../../entities'

export const create{{pascalCase name}} = {
  async create{{pascalCase name}}(_: any, { {{camelCase name}} }, context: any) {
    const { domain, user } = context.state

    return await getRepository({{pascalCase name}}).save({
      ...{{camelCase name}},
      domain,
      creator: user,
      updater: user
    })
  }
}

